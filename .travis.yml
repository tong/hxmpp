language: haxe

os:
  - linux

haxe:
  - "4.1.4"
  - development

matrix:
  allow_failures:
    - haxe: "development"

branches:
  only:
  - master
  - r2

install:
  - haxelib install hxnodejs
  - haxelib install utest
  - haxelib dev hxmpp .

script:
  - haxe haxedoc.hxml
  - haxe test.hxml
